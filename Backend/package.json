{
  "name": "fenghou-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "types": "wrangler types",
    "d1:migrate:local": "wrangler d1 migrations apply fenghou-device-data --local",
    "d1:migrate:remote": "wrangler d1 migrations apply fenghou-device-data --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260530.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.18.0"
  }
}
